version: '3'

services:
  nginx:
    build:
      context: .
    restart: unless-stopped
    volumes:
      - ./docker/nginx/conf.d:/etc/nginx/conf.d
      - ./:/var/www/crm-web/
    ports:
      - "8083:80"
    networks:
      - internal

networks:
  internal:
    driver: bridge